<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
    <title>MemoryMe</title>
</head>
<body>
        <header>
            <a href="{{ path('homepage') }}"><h1>MemoryMe</h1></a>
            {% if app.user %}
                <h2>L'application qui te permet de retenir tout ce que tu veux</h2>
                <nav>
                    <ul class="navbar">
                        <li class="linkHeader">
                            <a href="{{ path('others_cards_page') }}">Cartes</a>
                        </li>
                        <li class="linkHeader">
                            <a href="{{ path('Listcards_page') }}">Mes cartes</a>
                        </li>
                        <li class="linkHeader">
                            <a href="{{ path('account_page') }}" class="MyAccount">
                                    Mon Compte
                            </a>
                        </li>
                    </ul>
                </nav>
            {% endif %}
        </header>
        <main>
            {% block body %}

            {% endblock body %}
        </main>
        <footer>

        </footer>

</body>
</html>
